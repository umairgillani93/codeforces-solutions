#include <iostream>
using namespece std; 

int main() {
    int contests, scores 
    cout << "Enter contests : "
    cin >> contests 
    count << "Enter scores : "
    cin >> scores 
    int amount;
    for (i=0, i <=10, ++i) {
        for (j=0, j<=10, ++j) {
            if (i % 2 == 0) {
                cout << "Enter amount : "
                cin >> amount 
            }
        }
    }
}