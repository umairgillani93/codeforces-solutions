#include <bits/stdc++.h>

using namespace std;

int main() {
	int n, k, s;
	cin >> n >> k >> s;
	int t = 0; 
	int N = 0;
	vector<int> arr;
	for (int i = 1; i <= n; i++) {
		arr.push_back(i);
	}
	if (k >= n * (n + 1) / && k <= n * (n + 1) / 
	return 0;
}
