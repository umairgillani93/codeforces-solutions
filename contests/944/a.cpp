#include <bits/stdc++.h>

using namespace std;

int main() {
	int t;
	cin >> t;
	while (t--) {
		int x, y;
		cin >> x >> y;
		vector<int> arr;
		cout << min(x, y) << " " << max(x, y) << '\n';
	}
	return 0;
}
